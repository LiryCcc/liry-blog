# exp7 250722

## 八股

### `react` 17 18 区别

### `mobx` `redux`区别，用途，优缺点，为什么选择`mobx`

### 兼容性如何处理

### 如何优化加载速度

### 输入url到加载完成的过程

都是react

1. csr

2. ssr

### `webpack` `plugin` 在测试环境显示版本和`git`提交信息

## 做题

### `ahooks` 的 `useCountdown`同款写一个

能显示倒计时、重置事件、暂停、结束时出发回调

### `Promise` 依次调用，并带有重试次数

返回失败的`Promise`和或者最后一个`Promise`的结果
